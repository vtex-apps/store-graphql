scalar IOMessage

type Product {
  productName: String
  productId: String
}

type Query {
  """
  Returns a specified product
  """
  product(
    """
    Product slug
    """
    slug: String
  ): Product @cacheControl(scope: PRIVATE)

}
